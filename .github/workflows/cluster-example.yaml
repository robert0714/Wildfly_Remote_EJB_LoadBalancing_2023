name: cluster-example images ci

on:
  push:
    paths:
      - 'cluster-example-client/**'
      - 'cluster-example-server/**'
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Get current date
        id: date
        run: echo "::set-output name=date::$(date +'%Y-%m-%d')"
      - name: Set up JDK 8
        uses: actions/setup-java@v1
        with:
          java-version: 8  
      - name: Deploy to Github Package Registry
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        working-directory: ./
        run:  |
            echo $GITHUB_TOKEN | docker login ghcr.io -u USERNAME --password-stdin
            mvn install -Pwildfly
            
            docker build -f cluster-example-client/ --build-arg GITHUB_TOKEN=$GITHUB_TOKEN -t ghcr.io/robert0714/cluster-example-server:latest .
            docker build -f cluster-example-client/ --build-arg GITHUB_TOKEN=$GITHUB_TOKEN -t ghcr.io/robert0714/cluster-example-server:${{ steps.date.outputs.date }} .
            docker push ghcr.io/robert0714/cluster-example-client:latest
            docker push ghcr.io/robert0714/cluster-example-client:${{ steps.date.outputs.date }}

            docker build -f cluster-example-server/ --build-arg GITHUB_TOKEN=$GITHUB_TOKEN -t ghcr.io/robert0714/cluster-example-server:latest .
            docker build -f cluster-example-server/  --build-arg GITHUB_TOKEN=$GITHUB_TOKEN -t ghcr.io/robert0714/cluster-example-server:${{ steps.date.outputs.date }} .
            docker push ghcr.io/robert0714/cluster-example-server:latest
            docker push ghcr.io/robert0714/cluster-example-server:${{ steps.date.outputs.date }}